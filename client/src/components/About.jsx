import React, { PureComponent } from 'react';

export default class About extends PureComponent {
  render () {
    return (
      <div className="inner cover">
        <h1 className="cover-heading">Practicing WebDev!</h1>
        <p className="lead">Made to make a simple api, brush up on React, and then explore how to connect the two.  Also, it makes for a much cuter list of atari games than a spreadsheet...</p>
      </div>
    );
  }
}
